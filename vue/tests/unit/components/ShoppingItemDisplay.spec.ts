import { mount } from "@vue/test-utils";
import ShoppingCategory from "@/components/ShoppingCategory.vue";
import { Category } from "@/use/localApi";
import { Emitter } from "mitt";
// import jest from 'jest'
// import Greeting from "@/components/Greeting.vue";

describe("ShoppingItemDisplay", () => {
  it("renders an item 'in cart (text only)' when the item is in cart", () => {
    fail("test not implemented");
  });

  it("renders an editable item when the item is not cart", () => {
    fail("test not implemented");
  });
});
