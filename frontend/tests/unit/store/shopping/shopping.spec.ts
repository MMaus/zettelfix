import { mount } from "@vue/test-utils";

describe("The ShoppingList store", () => {
  it("creates categories as required when adding items", () => {
    fail("test not implemented");
  });

  it("puts an item into 'requested' state when adding an existing item", () => {
    fail("test not implemented");
  });

  // Actually, this is a ux decision: This disables having the the same item twice in different
  // categories.
  it("updates the category of an item as entered in the footer.", () => {
    fail("test not implemented");
  });
});
